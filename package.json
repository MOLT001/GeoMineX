{
  "name": "geominex",
  "version": "0.1.0",
  "private": true,
  "description": "GeoMineX — AI-assisted document intelligence platform for coal mining subsidiaries. Repository root.",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "dev:db": "npm --prefix backend run dev:db",
    "seed": "npm --prefix backend run seed --",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend run start",
    "test": "npm --prefix backend run test",
    "lint": "npm --prefix backend run lint",
    "typecheck": "npm --prefix backend run typecheck",
    "audit:ci": "npm --prefix backend run audit:ci",
    "install:all": "npm --prefix backend ci"
  }
}
